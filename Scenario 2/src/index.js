function hero(bullets, dragons){
     return (bullets/2 >= dragons)
  }

  // was it correct in the readme for scenario 2 --> each capable of slaying two dragons
  // I think this would be suitable --> two of them capable of slaying one dragons

//Use SpecRunner to check the Test Cases.